Values allow customization of charts.

Without values:
- Default settings used

With values:
- Override chart settings

Example:

replicaCount: 3

service:
  type: NodePort

Benefits:
- No need to edit templates
- Easy customization
- Production standard approach
