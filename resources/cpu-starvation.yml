apiVersion: v1
kind: Pod

metadata:
  name: cpu-starvation

spec:
  containers:
  - name: nginx
    image: nginx

    resources:
      requests:
        cpu: "20"
        memory: "128Mi"

      limits:
        cpu: "20"
        memory: "256Mi"
